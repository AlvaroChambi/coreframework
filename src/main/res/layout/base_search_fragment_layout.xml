<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="bottom|right"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/base_search_header_frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primary"
        android:elevation="4dp"
        android:padding="4dp"></FrameLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/base_floating_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:clickable="true"
            android:visibility="gone"
            android:layout_gravity="bottom|right"
            android:tint="@color/text_primary"
            app:srcCompat="@drawable/baseline_add_24" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/base_search_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:layout_marginBottom="8dp"
            android:scrollbars="vertical"></android.support.v7.widget.RecyclerView>

        <include
            android:id="@+id/base_request_loading_frame"
            layout="@layout/base_request_fragment_layout"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>